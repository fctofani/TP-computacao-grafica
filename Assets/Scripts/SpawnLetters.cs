using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnLetters : MonoBehaviour
{
    /*

    void Start()
    {
        InvokeRepeating("SpawnRandom", spawntime, spawndelay);
    }

    void SpawnRandom()
    {
        //console.log("INSANCIANDO");
        randomLetter = Random.Range(0, spawnLetters.Length);
        randomPosition = Random.Range(0, xPositions.Length);
        Instantiate(spawnLetters[randomLetter], new Vector3(randomPosition, 0, transform.position.z), Quaternion.identity);
    }

    void Update()
    {
        
    }

    */
}
